# Speaker labels — addendum

**For Noa and Judith. 2026-08-16.**

**This is an addition to the questionnaire you already have, not a replacement. Nothing in that document has changed — every candidate list and every tick-box is exactly as you received it, so please carry on answering it and keep whatever you have already marked.**

Two things were left out of the first version. Both are additions.

## 1\. Eight cast-list entries were missing

The first version asked only about **speech prefixes**. Eight questions concern entries in the **cast list** itself, and those were dropped. They are below.

They need a different question from a speech prefix. The usual problem is not that we cannot tell who it is, but that the tagged span is wrong: Meshumed's `א` covers only the article of `א ריכטיר פֿון געהיימס געריכט`, a role the cast list already contains. So these entries offer *the span is mis-cut* and *one span, several roles* as answers, rather than making you choose between names.

## 2\. Six labels occur slightly more often than shown

The counts in the version you have are a little low for six labels, because cast-list occurrences were not counted alongside speech ones:

| Play | Label | Shown | Actually |
| :---- | :---- | ----: | ----: |
| Meshumed | אסיפ | 62 | 65 |
| Yaakov-Esav | פאלק | 8 | 12 |
| Ben HaDor | אלטע | 2 | 4 |
| Ben HaDor | יונגע | 2 | 3 |
| Ben HaDor | ער | 7 | 8 |
| Meshumed | פאוויל | 1 | 2 |

**This changes nothing about how to answer them** — it is the same label in the same play, just a few more occurrences of it. Your answer still covers all of them.

One further detail, for Tissa-Essler's `קאהר`: the resolver reports *contains-ambiguous (2)*, meaning it found exactly **two** cast names equally plausible and declined to guess between them. Worth knowing when you pick.

---

## Di Tsvey Tnoim

*4 labels, 4 spans.* Cast list has 16 roles: `rbi_iukhnn_br_nskhh`, `izrelis`, `rbi_shmeun_bn_lkish`, `bn_fdus`, `tsenbi`, `hiln`, `br_nzir`, `khgle`, `nkhmn`, `fldsh`, `khr_fun_royber`, `snim` …

### Chor — 1 occurrence  *(castList `role` span)*

This is an entry in the **cast list**, not a speech prefix. It could not be given an `xml:id` — usually because one span covers several names, or names someone the list does not otherwise have. Should it be split, or is it one role?

**Pages:** [p.18](https://app.transkribus.org/collection/2372172/doc/838430/detail/18)

**Which role is this?**

*Nothing in this play's cast list resembles this label, so it is most likely a role that was never listed, or a collective.*

- [ ] **Mint a new role** — suggested `xml:id`: `chor` (name as printed: Chor)  
- [ ] **The span is mis-cut** — it covers the wrong text; the role itself is fine  
- [ ] **One span, several roles** — split it (say which in the comment)  
- [x] **Collective / group** — the cast list is naming a group here, not a person (`Chor`, `סאלדאטען`)  
- [ ] **Not a role** — this span is mis-tagged

**Comment:**

---

### בן פדות, אונד איזראעלית — 1 occurrence  *(castList `role` span)*

This is an entry in the **cast list**, not a speech prefix. It could not be given an `xml:id` — usually because one span covers several names, or names someone the list does not otherwise have. Should it be split, or is it one role?

**Pages:** [p.14](https://app.transkribus.org/collection/2372172/doc/838430/detail/14)

**In context: stage direction stating that both characters are going to perform a dialogue**

> בן פּדות, אונד איזראעלית. Auftr

**Which role is this?**

*No close match — the names below are simply the nearest in the cast list and may well be none of them. For a role like this, `mint a new role` or `collective` is often the answer.*

- [x] `izrelis` — יזראעלית  
- [x] `bn_fdus` — בן פדות  
- [ ] **Mint a new role** — suggested `xml:id`: `bn_fdus_und_izrelis` (name as printed: בן פדות, אונד איזראעלית)  
- [ ] **The span is mis-cut** — it covers the wrong text; the role itself is fine  
- [x] **One span, several roles** — split it (say which in the comment)  
- [ ] **Collective / group** — the cast list is naming a group here, not a person (`Chor`, `סאלדאטען`)  
- [ ] **Not a role** — this span is mis-tagged

Resolver: joint: 'אונד איזראעלית' unmatched. Where it says \*ambiguous\*, that is the number of cast names it found equally plausible — it declined to choose rather than guess.

**Comment: Split into two roles: bn\_fdus and izrelis** 

---

### כאהר. רויבער. חגלה, פלדש. נחמן. הילנא — 1 occurrence  *(castList `role` span)*

This is an entry in the **cast list**, not a speech prefix. It could not be given an `xml:id` — usually because one span covers several names, or names someone the list does not otherwise have. Should it be split, or is it one role?

**Pages:** [p.18](https://app.transkribus.org/collection/2372172/doc/838430/detail/18)

**In context:**

> כאָהר. רויבער. חגלה, פּלדש. נחמן. הילנא.

**Which role is this?**

*Nothing in this play's cast list resembles this label, so it is most likely a role that was never listed, or a collective.*

- [ ] **Mint a new role** — suggested `xml:id`: `khhr_royber_khglh_fldsh_nkhmn_hiln` (name as printed: כאהר. רויבער. חגלה, פלדש. נחמן. הילנא)  
- [ ] **The span is mis-cut** — it covers the wrong text; the role itself is fine  
- [x] **One span, several roles** — split it (say which in the comment)  
- [ ] **Collective / group** — the cast list is naming a group here, not a person (`Chor`, `סאלדאטען`)  
- [ ] **Not a role** — this span is mis-tagged

Resolver: joint: 'כאהר. רויבער. חגלה' unmatched. Where it says \*ambiguous\*, that is the number of cast names it found equally plausible — it declined to choose rather than guess.

**Comment: Split into two roles: bn\_fdus and izrelis** 

---

### שמעון \- צענאביא — 1 occurrence  *(castList `role` span)*

This is an entry in the **cast list**, not a speech prefix. It could not be given an `xml:id` — usually because one span covers several names, or names someone the list does not otherwise have. Should it be split, or is it one role?

**Pages:** [p.17](https://app.transkribus.org/collection/2372172/doc/838430/detail/17)

**In context: stage direction stating that both characters are going to perform a dialogue**

> Auft שמעון \- צענאַביאַ

**Which role is this?**

- [x] `tsenbi` — צענאביא  
- [x] `rbi_shmeun_bn_lkish` — רבי שמעון בן לקיש  
- [ ] **Mint a new role** — suggested `xml:id`: `shmeun_tsenbi` (name as printed: שמעון \- צענאביא)  
- [ ] **The span is mis-cut** — it covers the wrong text; the role itself is fine  
- [x] **One span, several roles** — split it (say which in the comment)  
- [ ] **Collective / group** — the cast list is naming a group here, not a person (`Chor`, `סאלדאטען`)  
- [ ] **Not a role** — this span is mis-tagged

**Comment: Split into two roles: rbi\_shmeun\_bn\_lkish and tsenbi** 

---

## Meshumed

*4 labels, 4 spans.* Cast list has 14 roles: `iekb_eyzenshteyn`, `iusf`, `mekhil`, `shrhke`, `sender`, `liubv_bulvn`, `nikli_nikitin`, `ivn`, `gregur_mrksenel`, `vsk`, `geheym_flitsist`, `rikhtir_fun_geheyms_gerikht` …

### א — 1 occurrence  *(castList `role` span)*

This is an entry in the **cast list**, not a speech prefix. It could not be given an `xml:id` — usually because one span covers several names, or names someone the list does not otherwise have. Should it be split, or is it one role?

**Pages:** [p.4](https://app.transkribus.org/collection/2372172/doc/534187/detail/4)

**In context: Part of the cast list at the beginning of the play** 

> א ריכטיר פֿון געהיימס געריכט

**Which role is this?**

- [ ] `vekhtir` — א וועכטיר  
- [ ] `shreybir_bey_gerikht` — א שרייביר ביי געריכט  
- [ ] `geheym_flitsist` — א געהיים פאליציסט  
- [ ] **Mint a new role** — suggested `xml:id`: `new_role` (name as printed: א)  
- [x] **The span is mis-cut** — it covers the wrong text; the role itself is fine  
- [ ] **One span, several roles** — split it (say which in the comment)  
- [ ] **Collective / group** — the cast list is naming a group here, not a person (`Chor`, `סאלדאטען`)  
- [ ] **Not a role** — this span is mis-tagged

**Comment: The span only caught 'א'. The full role is 'א ריכטיר פֿון געהיימס געריכט' (rikhtir\_fun\_geheyms\_gerikht)** 

---

### יעקב מעכעל שרה — 1 occurrence  *(castList `role` span)*

This is an entry in the **cast list**, not a speech prefix. It could not be given an `xml:id` — usually because one span covers several names, or names someone the list does not otherwise have. Should it be split, or is it one role?

**Pages:** [p.25](https://app.transkribus.org/collection/2372172/doc/534187/detail/25)

**In context: stage direction stating that both characters are going to perform** 

> יעקב מעכעל שרה (קאהר זינגט)

**Which role is this?**

*No close match — the names below are simply the nearest in the cast list and may well be none of them. For a role like this, `mint a new role` or `collective` is often the answer.*

- [x] `iekb_eyzenshteyn` — יעקב אייזענשטיין  
- [x] `mekhil` — מעכיל  
- [ ] **Mint a new role** — suggested `xml:id`: `iekb_mekhel_shrh` (name as printed: יעקב מעכעל שרה)  
- [ ] **The span is mis-cut** — it covers the wrong text; the role itself is fine  
- [x] **One span, several roles** — split it (say which in the comment)  
- [ ] **Collective / group** — the cast list is naming a group here, not a person (`Chor`, `סאלדאטען`)  
- [ ] **Not a role** — this span is mis-tagged

**Comment: Split into three roles: iekb\_eyzenshteyn, mekhil, and shrhke** 

---

### פאליסמאן — 1 occurrence  *(castList `role` span)*

This is an entry in the **cast list**, not a speech prefix. It could not be given an `xml:id` — usually because one span covers several names, or names someone the list does not otherwise have. Should it be split, or is it one role?

**Pages:** [p.38](https://app.transkribus.org/collection/2372172/doc/534187/detail/38)

**Which role is this?**

- [ ] `geheym_flitsist` — א געהיים פאליציסט  
- [ ] `ivn` — איוואן  
- [x] **Mint a new role** — suggested `xml:id`: `flismn` (name as printed: פאליסמאן)  
- [ ] **The span is mis-cut** — it covers the wrong text; the role itself is fine  
- [ ] **One span, several roles** — split it (say which in the comment)  
- [ ] **Collective / group** — the cast list is naming a group here, not a person (`Chor`, `סאלדאטען`)  
- [ ] **Not a role** — this span is mis-tagged

**Comment: This is a speaking character (Policeman/מוף אן) appearing in the text/scene dialogue, but missing from the initial cast list.** 

---

### שרה קינדער — 1 occurrence  *(castList `role` span)*

This is an entry in the **cast list**, not a speech prefix. It could not be given an `xml:id` — usually because one span covers several names, or names someone the list does not otherwise have. Should it be split, or is it one role?

**Pages:** [p.21](https://app.transkribus.org/collection/2372172/doc/534187/detail/21)

**Which role is this?**

- [x] `shrhke` — שרהקע  
- [ ] `sender` — סענדער  
- [ ] `shreybir_bey_gerikht` — א שרייביר ביי געריכט  
- [ ] **Mint a new role** — suggested `xml:id`: `shrh_kinder` (name as printed: שרה קינדער)  
- [ ] **The span is mis-cut** — it covers the wrong text; the role itself is fine  
- [x] **One span, several roles** — split it (say which in the comment)  
- [ ] **Collective / group** — the cast list is naming a group here, not a person (`Chor`, `סאלדאטען`)  
- [ ] **Not a role** — this span is mis-tagged

**Comment: Split into shrhke and kinder (collective)** 

---

