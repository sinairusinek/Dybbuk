# Speaker labels — what is left after the 08-16 questionnaire

**For Noa and Judith. 2026-08-30.**

Your answers to the speech-prefix questionnaire are in. 115 of 121 labels were
answered, and all of those are applied — 34 new roles minted, 55 spelling
variants folded onto roles that already existed, 9 collective labels added to
the shared collective map, 5 spans marked "not a speaker". Meshumed now has
**zero** flags needing a human; the other plays are down to a handful.

Six labels are still open. None of them is a spelling problem, which is why
none could be settled from the form.

---

## 1. Two labels where the answer was "I don't know"

### Yoysef in Egipten — `נאר`, p.19 (1 span)

The comment reads *לא יודעת כרגע*. Left exactly as it was.

Context: `נאר מיז שטערבען אויף דעם אַָרט`. Worth knowing that the very next
line on the same page is `סע מוז שטערבען אויף דעם ארט` — the same sentence
again — and you marked **that** one "not a speaker". If `סע` is a mis-tagged
fragment, `נאר` on the line above may well be the same thing rather than a
character: `נאר` is ordinarily the adverb "only/but". If you agree, both
spans get dropped and the question disappears.

- [ ] Not a speaker either — drop it, same as `סע`
- [ ] It **is** a speaker: _______________
- [ ] Still unsure — leave it flagged

---

## 2. Three labels where one span covers two roles

These need the span cut in two, not an id chosen. Same shape as the
`בן פדות, אונד איזראעלית` case you settled in the addendum, and both halves
already exist in the castList — so a yes is all that is needed.

### Emigration — `ירוחם שפרה`, p.47

> ירוחם שפרה אונזער קינד. וועה מיר. (אלגעמיינער לערם וויינען שרייען)

Your comment: *"Dual speaker header for Yeruham and Shifra speaking together."*

- [ ] Yes — split into `irukhm` + `shfrh`
- [ ] No, it is one label: _______________

### Ben HaDor — `צורהלפידות`, p.15

> צורהלפידות | יאָ יאָ רחומהלע דוא האסט רעכֿט

Your comment: *"Combined speaker label for Tsroyh and Lfidus singing together
in unison."* The printed form runs צרויה and לפידות together without a space.

- [ ] Yes — split into `tsroyh` + `lfidus`
- [ ] No, it is one label: _______________

### Di Tsvey Tnoim — `שמעון פדות`, p.9

> שמעון פּדות. יעצט הייסט עס אויס אנ׳נאַנדערגעהן

Here the tick and the comment disagree about **how many people are speaking**,
not about spelling, so neither reading was safe to assume. You ticked
`bn_fdus` (one person), but wrote *"Shimon and Pdos performing together in the
musical number. Tag as a combined group."*

Both readings are possible: the castList has `rbi_shmeun_bn_lkish` and
`bn_fdus` as separate characters, but `בן פדות`'s own full name contains
neither שמעון nor a conjunction.

- [ ] One speaker — `bn_fdus`, and שמעון is part of his name here
- [ ] Two speakers — split into `rbi_shmeun_bn_lkish` + `bn_fdus`
- [ ] Something else: _______________

---

## 3. Ben HaDor's duet pronouns — one question, not two

`ער` (8 spans) and `זיא` (3 spans). Your comments identify the *scenes* but
not the singers, and `ער` is used in **two different scenes**, so a single id
cannot cover it:

- **p.17 — the lovers' duet.** `ער` and `זיא` alternate.
  > ער | זעה איך נור דיך געליעבטע מיין
  > זיא | זעה איך נור דיך געליעבטער מיין
- **pp.32–33 — the comic trio**, where `ער` appears alongside `אלטע` (the old
  woman) and `יונגע` (the young woman), both now minted as roles.

For comparison, the same problem in the other three plays is already solved
and needs nothing from you: in Emigration the duet turned out to be Sanye and
Yekel (the couple married on pp.100–101 — `סאניע` was in the castList all
along as `snie`, and `אוי סאני פיינע` on p.113 is Yekel *addressing* her, not
naming himself); Tissa-Essler's p.22 duet is Moritz + Barbara, and Di Tsvey
Tnoim's p.46 duet names its own singers in the lines.

**Who sings on p.17?** The castList has `bn_hdur`, `sufur`, `tsroyh`,
`rkhumh`, `khnu`, `lfidus`, `shlmun`, `mun`, `elms`, `fisher` …

- ער = _______________  זיא = _______________

**And on pp.32–33, who is `ער` in the trio with אלטע and יונגע?**

- ער = _______________

If it is easier, just say which characters are on stage in each scene and the
per-scene overrides can be written from that — nothing needs retagging.

---

*Everything else from the 08-16 questionnaire is done. Nothing above blocks
the rest of the pipeline; these six are the only spans still unresolved.*
